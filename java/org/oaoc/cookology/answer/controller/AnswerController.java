package org.oaoc.cookology.answer.controller;

import org.oaoc.cookology.answer.model.service.AnswerService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

public class AnswerController {
	
	/*
	 * private static final Logger logger =
	 * LoggerFactory.getLogger(AnswerController.class);
	 * 
	 * @Autowired // 자동 의존성 주입(DI) : 자동 객체 생성됨 private AnswerService answerService;
	 * // 로그인 페이지 내보내기용 메소드
	 * 
	 * @RequestMapping(value="", method= { RequestMethod.GET, RequestMethod.POST })
	 * public String move() { return ""; }
	 */
}
